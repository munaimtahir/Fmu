# =============================================================
# KEYSTONE DEPLOYMENT CONFIGURATION
# =============================================================
# For Keystone deployments with path-based routing (e.g., http://VPS_IP/{APP_SLUG}/)
# 
# VITE_BASE_PATH: The subpath where the app is mounted (Keystone routing)
#   - Local dev: Leave empty or set to '/' (default)
#   - Keystone: Set to '/{APP_SLUG}/' (e.g., '/sims/' or '/fmu/')
#   IMPORTANT: Must start and end with '/'
VITE_BASE_PATH=/

# VITE_API_URL: Base URL for the Django API
#   - Local dev with separate backend: http://localhost:8000
#   - Local dev with proxy: Leave empty (uses Vite proxy)
#   - Keystone deployment: Leave empty (backend is at same domain, different path)
#   - Production without Keystone: Leave empty for relative URLs
VITE_API_URL=http://localhost:8000
